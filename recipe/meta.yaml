{% set name = "explainerdashboard" %}
{% set version = "0.2.19" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/explainerdashboard-{{ version }}.tar.gz
  sha256: c4e0d4cc200ae737f546828acd964f74e51c70763f01d00fc3992bd7687b2191

build:
  number: 0
  noarch: python
  entry_points:
    - explainerdashboard = explainerdashboard.cli:explainerdashboard_cli
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python
  run:
    - click
    - dash
    - dash-auth
    - dash-bootstrap-components
    - dtreeviz >=1.0
    - joblib
    - jupyter-dash
    - numpy
    - oyaml
    - pandas
    - pdpbox
    - python
    - scikit-learn
    - shap >=0.36
    - shortuuid

test:
  imports:
    - explainerdashboard
    - explainerdashboard.dashboard_components
  commands:
    - pip check
    - explainerdashboard --help
  requires:
    - pip

about:
  home: https://github.com/oegedijk/explainerdashboard
  summary: explainerdashboard allows you quickly build an interactive dashboard to explain the inner workings of your machine learning model.
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - raybellwaves
    - oegedijk
